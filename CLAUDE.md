# CLAUDE.md — Couponsnoor

قواعد عمل لأي جلسة Claude Code على هذا المشروع. موقع كوبونات عربي (Next.js 15 App Router + React 19 + Prisma + Supabase)، اللغة الافتراضية والوحيدة حاليًا عربي RTL (`lang="ar" dir="rtl"` بـ `src/app/layout.tsx`).

## SEO

- كل Metadata يمر عبر `src/lib/seo.ts` (`buildMetadata` + `storeMetadata`/`couponMetadata`/`categoryMetadata`). أي صفحة جديدة لازم تستخدم نفس النمط، مو `metadata` يدوية مبعثرة بكل `page.tsx`.
- كوبونات منتهية الصلاحية أو غير منشورة تصير `noindex` تلقائيًا (`isExpired` بـ `seo.ts`) — لا تلغي هالمنطق.
- صفحة الكوبون عمدًا **بدون** `ogImage` يدوي بـ `couponMetadata` — بتلتقط تلقائيًا `opengraph-image.tsx` الديناميكي بنفس المسار (راجع قسم "صور الكوبونات" تحت). لا تضيف `ogImage` يدوي هناك إلا إذا قررنا نلغي الصورة الديناميكية عمدًا.
- ما فيه نسخة `/en` فعلية — `hreflang="en"` اتشالت قصدًا لأنها كانت تأشر لروابط 404 (تعليق بـ `seo.ts`). لا تعيدها إلا لما تصير فيه نسخة إنجليزية حقيقية.
- JSON-LD (breadcrumb/FAQ/Offer) موجودة جاهزة بـ `seo.ts` — استخدمها، لا تكتب schema يدوي بالصفحة.
- `revalidate = 3600` على صفحات الكوبون/المتجر (ISR) — أي تغيير بمنطق العرض لازم يحترم هالكاش، مو `no-store` عشوائي.

## Next.js

- Next 15 App Router — أي `params` أو `searchParams` بـ Server Component أو Route Handler هو **Promise**، لازم `await params` (مو `params.slug` مباشرة).
- الوصول لقاعدة البيانات دايمًا عبر `src/lib/db` (Prisma singleton) — ما تسوي `new PrismaClient()` بأي مكان تاني، هيك صار عنا Prisma pool exhaustion سبّب فشل بناء متقطع على Vercel (راجع commit `24b4df8`).
- `generateStaticParams` بصفحة الكوبون يبني أول 200 كوبون فقط static — الباقي on-demand ISR، عادي.
- Route handlers الخاصة بتوليد الصور (`opengraph-image.tsx`, أو أي `route.tsx` مشابه) لازم تبقى ملفات Next الخاصة بنفس الاسم، لا تغيّر مكانها أو تحوّلها لدالة عادية.
- قبل أي تغيير بـ TypeScript/TSX: شغّل `npx tsc --noEmit` — ما تعتبر الشغلة خلصت قبل ما يطلع نضيف.

## الأداء (Performance)

- تحويل شعارات المتاجر WebP→PNG يصير بالذاكرة عبر `sharp` (`logoToPngDataUri` بـ `src/lib/coupon-og.tsx`) — Satori ما بيفك WebP. لا تشيل هالخطوة ولا تحاول تمرر WebP مباشرة لـ `<img>` جوا `ImageResponse`.
- خط Tajawal لصور OG محمّل ومكاش بالذاكرة (module-level `Map` بـ `src/lib/og-font.ts`) — أي صورة OG جديدة لازم تستخدم `getTajawalBold()` الموجودة، مو تجيب الخط من جديد كل مرة.
- لا تضيف مكتبات ثقيلة (chart libs، animation libs ضخمة، إلخ) لصفحات عامة بدون داعي واضح — الموقع محتاج يضل خفيف عشان Core Web Vitals/SEO.
- أي صورة مولّدة (`next/og`) لازم تُختبر فعليًا بتشغيل السيرفر محليًا وجلب الصورة، مو افتراض إنها هتشتغل صح من قراءة الكود بس.

## التصميم (Design)

- مصدر الحقيقة الوحيد للألوان/الخطوط/الـ radius/الظلال هو `tailwind.config.ts` — ولا لون أو قيمة "طالعة من فراغ" بأي مكون أو صورة OG. الألوان الأساسية حاليًا: كحلي `primary` (`#14213D`)، كورال `accent` (`#CD3018`)، خلفيات `surface`/`bg` بيضاء/أوف-وايت.
- خط العربي بكل الموقع (والمكونات) هو Tajawal — أي نص عربي بمكوّن React عادي بياخد الخط تلقائيًا عبر `font-body`/`font-display`؛ بس صور `next/og` المولّدة لازم تمرر بيانات الخط يدويًا (راجع `og-font.ts`).
- عناصر UI جديدة (أزرار، بطاقات، حقول) لازم تسحب من design tokens الموجودة (`rounded-sm/md/lg`, `shadow-sm/md/lg`, ألوان Tailwind) مو قيم CSS خام.

## الاختبارات (Tests)

- ما فيه إطار اختبارات آلي (لا Jest ولا Playwright ولا Vitest) بالمشروع حاليًا — التحقق يصير يدويًا:
  1. `npx tsc --noEmit` بعد أي تعديل TypeScript.
  2. لأي تغيير UI: شغّل `npm run dev` فعليًا وافتح/جلب الصفحة، لا تكتفي بمراجعة الكود.
  3. لأي تعديل على صور `next/og`: ولّد الصورة فعليًا وافحصها (Read tool يقدر يعرض PNG مباشرة) — لا تفترض النتيجة من الكود وبس.
- إذا انضاف إطار اختبارات لاحقًا، حدّث هالقسم فورًا بأمر التشغيل الصحيح.

## صور الكوبونات (Coupon OG Images)

- كل عناصر التصميم المشتركة (الألوان، `Sparkle`، `CopyIcon`، `logoToPngDataUri`، `ArabicText`) موجودة بـ `src/lib/coupon-og.tsx` — أي صورة OG جديدة أو معدّلة لازم تستوردها من هناك، ما تكرر الكود.
- صورة كل كوبون: `src/app/store/[storeSlug]/coupon/[couponSlug]/opengraph-image.tsx` — مربّع 400×400، هاي التصميم النهائي المطبّق على كل المتاجر (مو تجربة محصورة بمتجر واحد بعد الآن).
- صورة OG الافتراضية لباقي الموقع (الرئيسية، التصنيفات، الصفحات الثابتة): `src/app/opengraph-image.tsx` — 1200×630.
- **قاعدة حرجة:** Satori (محرّك `next/og`) بيشكّل حروف كل كلمة عربية صح لحالها، لكن **ما بيعيد ترتيب كلمات الجملة العربية** (bidi عالمستوى الجملة). أي نص عربي بأكثر من كلمة داخل صورة `ImageResponse` **لازم** يمر عبر `ArabicText` (بـ `coupon-og.tsx`) وإلا رح يطلع الترتيب معكوس بصريًا حتى لو كل كلمة لحالها صحيحة. لا تعتمد على `direction: "rtl"` وحدها — جُرّبت وما أثّرت.
- التحقق من صحة اتجاه النص العربي بصورة مولّدة **ممنوع** يصير بقراءة بصرية عابرة (ثبت إنها غير موثوقة أكتر من مرة بهالمشروع) — لازم: (أ) قصّ الصورة وتكبيرها، و(ب) الاستناد لعنصر ثابت الموقع (زخرفة، خط فاصل، صندوق شعار) كمرجع يمين/يسار، أو تحليل بكسلات (مسح الأعمدة عن لون النص) لتحديد حدود كل كلمة بالضبط.

## دعم العربية RTL

- `dir="rtl"` و`lang="ar"` مضبوطين على مستوى `<html>` بـ `src/app/layout.tsx` — أي مكوّن جديد يعتمد على هالاتجاه تلقائيًا، ما تفرض `dir="ltr"` يدوي إلا لعنصر لازم يبقى LTR فعلًا (كود الكوبون، أرقام، نص إنجليزي صرف).
- أسماء المتاجر أحيانًا مزيج عربي/إنجليزي (زي "iHerb - ايهيرب") — أي منطق ترتيب نص (بما فيها `ArabicText`) رح يعكس السلسلة كاملة إذا فيها أكتر من كلمة، حتى لو جزء منها إنجليزي. خذ هالتأثير بعين الاعتبار قبل ما تطبّق أي حل عام على نص مختلط.
- كود الكوبون نفسه (أحرف/أرقام لاتينية زي `DGK0899`) ما بينلمس أبدًا بأي منطق RTL — يضل نص عادي LTR داخل الصف.

## تقليل استهلاك الـ Context والتوكنز

- ما تقرأ ملفات كاملة كبيرة لما تحتاج جزء منها بس — استخدم `Grep`/`offset`+`limit` بـ `Read` بدل قراءة الملف كامل.
- ما تعيد قراءة ملف عدلته توًا بـ `Edit`/`Write` للتأكد — الأداة نفسها بترجع خطأ لو العملية فشلت.
- لأي بحث استكشافي واسع (عدة ملفات، مسار مو واضح مكانه) فضّل `Agent`/fork بدل ما تحشر نتائج بحث ضخمة بالسياق الرئيسي.
- لأي صورة/سكرينشوت للتحقق البصري: اقصّ (`sharp`) على المنطقة المطلوبة بس قبل ما تعرضها، ما تعرض صور كاملة كبيرة لما مقطع صغير كافي.
- لا تولّد ملفات توثيق/تخطيط وسيطة (مسودات، خطط، ملخصات) إلا إذا طُلبت صراحة — اشتغل من سياق المحادثة مباشرة.
